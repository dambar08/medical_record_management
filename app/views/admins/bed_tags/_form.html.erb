<%= form_with(model: [:admins, bed_tag]) do |form| %>
  <% if bed_tag.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bed_tag.errors.count, "error") %> prohibited this bed_tag from being saved:</h2>

      <ul>
        <% bed_tag.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
