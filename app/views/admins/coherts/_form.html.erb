<%= form_with(model: [:admins, cohert]) do |form| %>
  <% if cohert.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cohert.errors.count, "error") %> prohibited this cohert from being saved:</h2>

      <ul>
        <% cohert.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
